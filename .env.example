# Optional AWS Lambda price updates; disabled by default.
BACKTEST_ENABLE_LAMBDA=0
BACKTEST_AWS_REGION=us-east-1
BACKTEST_LAMBDA_FUNCTION_NAME=getPriceDelta

# Credentials are loaded by boto3's standard credential chain.
# Never place AWS access keys or secret keys in this file.
